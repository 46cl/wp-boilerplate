<sequential-boxes name="{{ name }}"
                  fields="{{ fields | json_encode | escape('html_attr') }}"
                  data="{{ data | json_encode | escape('html_attr') }}"
                  options="{{ options | json_encode | escape('html_attr') }}">
</sequential-boxes>
